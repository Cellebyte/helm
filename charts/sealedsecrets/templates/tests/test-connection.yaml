apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sealedsecrets.fullname" . }}-test-connection"
  labels:
    {{- include "sealedsecrets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sealedsecrets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
