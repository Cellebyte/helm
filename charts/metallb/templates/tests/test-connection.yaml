apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "DTMetalLB.fullname" . }}-test-connection"
  labels:
{{ include "DTMetalLB.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "DTMetalLB.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
